# Руководство по развертыванию Ewabotjur на Amvera

Полное руководство по развертыванию бота-юриста на платформе Amvera с интеграцией Telegram, Bitrix24, DaData и OpenAI GPT.

## 📋 Содержание

- [Подготовка репозитория](#подготовка-репозитория)
- [Создание приложения в Amvera](#создание-приложения-в-amvera)
- [Настройка переменных окружения](#настройка-переменных-окружения)
- [Настройка Telegram webhook](#настройка-telegram-webhook)
- [Настройка Bitrix24 OAuth](#настройка-bitrix24-oauth)
- [Проверка развертывания](#проверка-развертывания)

## 🚀 Подготовка репозитория

Репозиторий уже подготовлен для развертывания на Amvera:

### Конфигурационные файлы

1. **Dockerfile** - мультистейдж сборка для оптимизации образа
2. **amvera.yml** - конфигурация порта для Amvera (порт 3000)
3. **.env.example** - шаблон переменных окружения

### Архитектура приложения

```
┌─────────────┐
│  Telegram   │ ──── HTTPS Webhook ───┐
└─────────────┘                       │
                                      ▼
┌─────────────┐              ┌──────────────┐
│  Bitrix24   │ ──── OAuth ──▶   FastAPI    │
│   imbot     │              │  (port 3000) │
└─────────────┘              └──────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
              ┌──────────┐      ┌──────────┐     ┌──────────┐
              │  DaData  │      │ OpenAI   │     │ Storage  │
              │   API    │      │   GPT    │     │  (JSON)  │
              └──────────┘      └──────────┘     └──────────┘
```

### Endpoints

Приложение предоставляет следующие endpoints:

- `GET /health` - healthcheck
- `POST /webhook/telegram/<SECRET>` - Telegram webhook
- `POST /webhook/bitrix` - Bitrix24 события
- `GET /oauth/bitrix/callback` - OAuth callback для Bitrix24

## 🏗️ Создание приложения в Amvera

### Шаг 1: Создайте новое приложение

1. Войдите в [Amvera](https://amvera.ru)
2. Нажмите **Create application**
3. Выберите источник: **GitHub**
4. Выберите репозиторий: `ArtemFilin1990/Ewabotjur`
5. Выберите ветку: `main` (или вашу рабочую ветку)

### Шаг 2: Настройте Toolchain

- **Toolchain**: Docker (автоматически определяется по Dockerfile)
- Amvera автоматически использует `amvera.yml` для конфигурации порта

### Шаг 3: Запустите деплой

- Нажмите **Deploy**
- Дождитесь завершения сборки и запуска

После успешного деплоя вы получите URL вида:
```
https://<app-name>.amvera.app
```

## 🔧 Настройка переменных окружения

В Amvera перейдите в раздел **Переменные** вашего приложения и добавьте следующие переменные:

### Обязательные переменные

#### Telegram
```bash
TELEGRAM_BOT_TOKEN=<your_bot_token>
TG_WEBHOOK_SECRET=<random_secret_string>
ALLOWED_CHAT_IDS=<chat_id_1>,<chat_id_2>
```

**Как получить:**
- `TELEGRAM_BOT_TOKEN`: создайте бота через [@BotFather](https://t.me/BotFather)
- `TG_WEBHOOK_SECRET`: сгенерируйте случайную строку (32+ символов)
- `ALLOWED_CHAT_IDS`: отправьте сообщение боту и используйте `getUpdates`:
  ```bash
  curl "https://api.telegram.org/bot<TOKEN>/getUpdates"
  ```

#### DaData
```bash
DADATA_TOKEN=<your_api_token>
DADATA_SECRET=<your_secret_key>
```

**Как получить:** зарегистрируйтесь на [dadata.ru](https://dadata.ru) и получите API ключи

#### OpenAI
```bash
OPENAI_API_KEY=<your_openai_api_key>
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2000
```

**Как получить:** создайте ключ на [platform.openai.com](https://platform.openai.com/api-keys)

#### Bitrix24
```bash
BITRIX_DOMAIN=https://your-domain.bitrix24.ru
BITRIX_CLIENT_ID=<your_client_id>
BITRIX_CLIENT_SECRET=<your_client_secret>
BITRIX_REDIRECT_URL=https://<app-name>.amvera.app/oauth/bitrix/callback
```

**Как получить:** создайте приложение в Bitrix24 (см. раздел ниже)

### Опциональные переменные

```bash
APP_NAME=jurist-bot
APP_ENV=production
LOG_LEVEL=INFO
HTTP_TIMEOUT_SECONDS=15
MAX_FILE_SIZE_MB=15
MEMORY_STORE_PATH=./storage/memory.json

# MCP (опционально)
USE_MCP=false
MCP_SERVER_URL=
MCP_API_KEY=
```

### ⚠️ Важно

После добавления/изменения переменных окружения **обязательно перезапустите контейнер** в Amvera UI. Переменные не применяются на этапе сборки - только на runtime.

## 📱 Настройка Telegram webhook

После успешного деплоя настройте webhook для Telegram бота:

```bash
curl -s "https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/setWebhook" \
  -d "url=https://<app-name>.amvera.app/webhook/telegram/<TG_WEBHOOK_SECRET>"
```

**Проверьте webhook:**
```bash
curl -s "https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/getWebhookInfo"
```

Должно вернуться:
```json
{
  "ok": true,
  "result": {
    "url": "https://<app-name>.amvera.app/webhook/telegram/<SECRET>",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

## 🔐 Настройка Bitrix24 OAuth

### Шаг 1: Создайте приложение в Bitrix24

1. Откройте ваш портал Bitrix24
2. Перейдите в **Приложения** → **Разработчикам** → **Другое** → **Локальное приложение**
3. Создайте новое приложение

### Шаг 2: Настройте права приложения

Требуемые права:
- `imbot` - для отправки сообщений в чат
- `im` - для работы с мессенджером

### Шаг 3: Настройте URL callback

В настройках приложения укажите:
```
URL для получения кода: https://<app-name>.amvera.app/oauth/bitrix/callback
```

### Шаг 4: Получите CLIENT_ID и CLIENT_SECRET

После создания приложения скопируйте:
- **CLIENT_ID** → `BITRIX_CLIENT_ID`
- **CLIENT_SECRET** → `BITRIX_CLIENT_SECRET`

### Шаг 5: Авторизуйте приложение

1. Откройте в браузере URL для авторизации (получите из логов или сформируйте вручную):
   ```
   https://<bitrix-domain>/oauth/authorize/?client_id=<CLIENT_ID>&response_type=code&redirect_uri=https://<app-name>.amvera.app/oauth/bitrix/callback
   ```

2. Разрешите доступ приложению

3. Вы будете перенаправлены на `/oauth/bitrix/callback`, где произойдет обмен кода на токены

4. **TODO**: Реализуйте сохранение токенов в защищенное хранилище (БД, зашифрованный файл)

### Использование Bitrix24 API

После авторизации можно отправлять сообщения через imbot:

```python
from src.integrations.bitrix24 import BitrixOAuthClient, BitrixAPIClient, BitrixTokens

# Получите токены из хранилища
tokens = BitrixTokens(
    access_token="...",
    refresh_token="...",
    expires_at=...,
    domain="https://your-domain.bitrix24.ru"
)

# Создайте API клиент
api_client = BitrixAPIClient(tokens)

# Отправьте сообщение
await api_client.send_message(
    dialog_id="chatXXX",  # или user_id
    message="Анализ контрагента завершен..."
)
```

## ✅ Проверка развертывания

### 1. Проверьте healthcheck

```bash
curl https://<app-name>.amvera.app/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "app": "jurist-bot",
  "environment": "production"
}
```

### 2. Проверьте Telegram webhook

Отправьте сообщение боту в Telegram. Проверьте логи в Amvera UI.

### 3. Проверьте обработку ИНН

Отправьте боту команду:
```
/company_check 7707083893
```

Бот должен:
1. Запросить данные из DaData
2. Проанализировать через OpenAI GPT
3. Вернуть структурированный анализ рисков

### 4. Проверьте логи

В Amvera UI откройте **Логи** и убедитесь, что нет критических ошибок.

## 🔍 Отладка проблем

### Webhook не работает

1. Проверьте, что `TG_WEBHOOK_SECRET` совпадает в переменных и URL webhook
2. Проверьте логи на наличие ошибок валидации
3. Убедитесь, что URL webhook начинается с `https://`

### Ошибки авторизации Bitrix24

1. Проверьте, что `BITRIX_REDIRECT_URL` точно совпадает с URL в настройках приложения
2. Убедитесь, что права приложения включают `imbot` и `im`
3. Проверьте логи на наличие ошибок OAuth

### DaData возвращает пустые данные

1. Проверьте баланс аккаунта DaData
2. Убедитесь, что тариф включает нужные поля (ИНН, ОГРН, руководитель и т.д.)
3. Проверьте правильность `DADATA_TOKEN` и `DADATA_SECRET`

### OpenAI ошибки

1. Проверьте баланс OpenAI API
2. Убедитесь, что модель `gpt-4o-mini` доступна в вашем аккаунте
3. Проверьте валидность `OPENAI_API_KEY`

## 📊 Мониторинг

### Логирование

Все компоненты логируют события в структурированном JSON формате с полями:
- `module` - название модуля
- `status` - статус операции (success, error, warning)
- `request_id` - ID запроса для трейсинга

### Метрики

Рекомендуется настроить мониторинг:
- Количество обработанных webhook запросов
- Задержка ответов DaData/OpenAI
- Количество ошибок авторизации Bitrix24
- Использование токенов OpenAI

## 🔄 Обновления

### Деплой изменений

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub
3. В Amvera UI нажмите **Redeploy** или настройте автодеплой

### Откат версии

В Amvera UI можно откатиться на предыдущую версию деплоя.

## 📚 Дополнительные ресурсы

- [Документация Amvera](https://docs.amvera.ru/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Bitrix24 REST API](https://dev.1c-bitrix.ru/rest_help/)
- [DaData API](https://dadata.ru/api/)
- [OpenAI API](https://platform.openai.com/docs/api-reference)

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в Amvera UI
2. Изучите раздел "Отладка проблем" выше
3. Откройте issue в GitHub репозитории
