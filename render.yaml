services:
  - type: web
    name: jurist-worker
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.worker_main
    envVars:
      - key: LOG_LEVEL
        value: INFO
