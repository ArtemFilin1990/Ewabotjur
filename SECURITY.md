# Правила безопасности Ewabotjur

## 🔒 Общие принципы

1. **Никогда не коммитьте секреты в репозиторий**
   - Токены
   - API ключи
   - Пароли
   - Сертификаты

2. **Все секреты через переменные окружения**
   - Используйте `os.getenv()` для получения значений
   - Никогда не хардкодьте секреты в коде
   - Используйте `.env` локально, переменные окружения на продакшене

3. **Логирование**
   - Никогда не логируйте полные значения токенов
   - Используйте `has_token: true/false` вместо значений
   - Не логируйте персональные данные пользователей

## 🛡️ Защита веб-приложения

### Webhook Security

**Telegram:**
- ✅ Используйте секретный ключ в URL: `/webhook/telegram/{secret}`
- ✅ Проверяйте секрет перед обработкой
- ✅ Возвращайте 403 при неверном секрете

**Bitrix24:**
- ✅ Проверяйте подпись запросов (если доступна)
- ✅ Валидируйте формат данных

### OAuth Security

**Bitrix24 OAuth:**
- ✅ Храните refresh_token безопасно
- ✅ Автоматически обновляйте access_token при истечении
- ✅ Используйте HTTPS для всех OAuth запросов
- ✅ Проверяйте redirect_uri

## 🔐 Хранение токенов

### В разработке
```python
# storage/bitrix_tokens.json (в .gitignore)
{
  "access_token": "...",
  "refresh_token": "...",
  "expires_in": 3600,
  "saved_at": "2024-01-01T00:00:00"
}
```

### В продакшене
Рекомендуется использовать:
- База данных с шифрованием
- Secret management системы (AWS Secrets Manager, HashiCorp Vault, и т.д.)
- Зашифрованные переменные окружения

## 🚫 Что НЕ делать

### ❌ НЕПРАВИЛЬНО:

```python
# Хардкод секретов
TELEGRAM_TOKEN = "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"

# Логирование токенов
logger.info(f"Token: {token}")

# Коммит .env файла
git add .env
git commit -m "Add config"
```

### ✅ ПРАВИЛЬНО:

```python
# Использование переменных окружения
import os
TELEGRAM_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

# Безопасное логирование
logger.info(f"Has token: {bool(token)}")

# .env в .gitignore
# Только .env.example в репозитории
```

## 🔍 Проверка безопасности

Перед коммитом:
1. ✅ Проверьте, что `.env` в `.gitignore`
2. ✅ Убедитесь, что нет хардкода секретов
3. ✅ Проверьте логи на наличие чувствительных данных
4. ✅ Убедитесь, что токены не в git истории

```bash
# Поиск потенциальных секретов
git diff --cached | grep -i "token\|secret\|password\|api_key"
```

## 📋 Checklist для деплоя

- [ ] Все секреты в переменных окружения
- [ ] `.env` в `.gitignore`
- [ ] HTTPS включен (Amvera обеспечивает автоматически)
- [ ] Webhook secret настроен
- [ ] OAuth redirect URL настроен правильно
- [ ] Логи не содержат секретов
- [ ] Токены хранятся безопасно
- [ ] Минимальные права доступа

## 🆘 Если секрет утёк

1. **Немедленно** отзовите скомпрометированный токен/ключ
2. Сгенерируйте новый
3. Обновите переменные окружения
4. Проверьте логи на предмет несанкционированного использования
5. Если токен в git истории — используйте BFG Repo-Cleaner или git filter-branch

## 📞 Контакты по безопасности

При обнаружении уязвимостей:
- Создайте приватный issue
- Не публикуйте детали публично до исправления
- Следуйте принципу responsible disclosure

## 🔄 Регулярные проверки

- Еженедельно: проверка логов на утечки
- Ежемесячно: ротация ключей API
- Ежеквартально: аудит безопасности кода
