# Ewabotjur - Telegram Legal Bot

Телеграм-бот юриста с интеллектуальной маршрутизацией, обработкой документов и генерацией профессиональных артефактов.

## Архитектура (Vercel + Render)

- **Vercel** принимает Telegram webhook на `/api/telegram` и всегда отвечает `200 OK` (без тяжёлой логики).
- **Render** запускает долгоживущий worker (FastAPI), который обрабатывает команды, файлы, DaData и отправляет ответы пользователям.
- Вебхук на Vercel пересылает исходный update JSON в Render worker: `POST ${RENDER_WORKER_URL}/ingest` с `Authorization: Bearer ${WORKER_AUTH_TOKEN}`.

## Быстрый старт (локально)

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
export TELEGRAM_BOT_TOKEN=...
export WORKER_AUTH_TOKEN=...
uvicorn src.app:app --host 0.0.0.0 --port 8000
```

## Переменные окружения

### Render (worker)

- `TELEGRAM_BOT_TOKEN` — токен Telegram Bot API.
- `WORKER_AUTH_TOKEN` — общий секрет между Vercel и Render.
- `ALLOWED_CHAT_IDS` — список ID через запятую (пусто = публичный режим).
- `DADATA_TOKEN` — токен DaData.
- `DADATA_SECRET` — секрет DaData (опционально).
- `HTTP_TIMEOUT_SECONDS` — таймаут внешних вызовов.
- `MAX_FILE_SIZE_MB` — лимит размера файлов (по умолчанию 15).
- `MEMORY_STORE_PATH` — путь к JSON-хранилищу памяти.
- `LOG_LEVEL`, `APP_NAME`, `APP_ENV`, `TELEGRAM_API_BASE`, `ENABLE_OCR`.

### Vercel (webhook)

- `RENDER_WORKER_URL` — URL сервиса Render (например, `https://jurist-worker.onrender.com`).
- `WORKER_AUTH_TOKEN` — тот же секрет, что и на Render.
- `TELEGRAM_WEBHOOK_SECRET` — опциональный секрет для заголовка `X-TG-SECRET`.

## Настройка webhook

```bash
curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://<vercel-domain>/api/telegram","secret_token":"<X-TG-SECRET>"}'

curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getWebhookInfo"
```

## Развёртывание

### Render

1. Создайте новый Web Service.
2. Укажите `Build Command`: `pip install -r requirements.txt`.
3. Укажите `Start Command`: `uvicorn src.app:app --host 0.0.0.0 --port $PORT`.
4. Добавьте переменные окружения из раздела Render.

### Vercel

1. Импортируйте репозиторий как Next.js проект.
2. Добавьте переменные окружения из раздела Vercel.
3. Убедитесь, что маршрут `/api/telegram` доступен.

## ASSUMPTIONS

- Worker реализован на Python (FastAPI), так как базовый код в репозитории уже использует Python.
- Для признаков «массового адреса/руководителя» используются поля `is_mass` из DaData, если они есть в ответе.

## 🎯 Реализованные функции

### ✅ Модуль 1: Безопасность (Security Layer)
- **Whitelist Middleware**: Контроль доступа по ID пользователей
- Настройка через переменную окружения `ALLOWED_CHAT_IDS`
- Поддержка как открытого режима (public), так и ограниченного (corporate)

```python
from src.security.access_control import AccessController

controller = AccessController("123456,789012")
if controller.is_allowed(user_id):
    # Обработка запроса
```

### ✅ Модуль 2: Обработка файлов (File Ingestion)
- **Извлечение текста** из PDF, DOCX, TXT, MD
- Поддержка как файловых путей, так и BytesIO буферов
- Защита от переполнения контекста (лимит 50,000 символов)
- Graceful degradation при отсутствии библиотек

```python
from src.files.index import FileProcessor

processor = FileProcessor()
text = processor.extract_text("contract.pdf")
# или
text = processor.extract_text(file_buffer, ext=".docx")
```

### ✅ Модуль 3: Интеллектуальная маршрутизация (LLM Router)
- **Детерминированный роутер** с hard gates и soft matching
- Порог уверенности 75% для автоматического запуска
- 10 сценариев: 9 канонических + DaData
- Система уточняющих вопросов при низкой уверенности

**Hard Gates (приоритетная маршрутизация):**
- Таблица рисков: "таблица рисков", "риски договора"
- Договор: "проверь договор", "составь договор"
- Претензия: "ответ на претензию"
- DaData: "ИНН", "ОГРН", "проверить компанию"
- И другие...

```python
from src.router.index import Router

router = Router()
scenario, confidence = router.route("Создай таблицу рисков")
# → (Scenario.RISK_TABLE, 0.95)

if router.is_confident(confidence):
    # Запускаем автоматически
else:
    # Задаём уточняющие вопросы
    questions = router.get_clarifying_questions(scenario)
```

### ✅ Модуль 4: Промпты с Jinja2 (Canonical Prompts)
- **Template Engine**: Jinja2 для гибкости
- Готовые шаблоны для ключевых сценариев
- Автоматическая подстановка контекста (компании, документы, данные)
- Fallback на базовые промпты

**Доступные шаблоны:**
- `risk_table.j2` - Анализ рисков договора
- `contract_agent_rf.j2` - Проверка договора по ГК РФ
- `claim_response.j2` - Ответ на претензию
- `legal_document_structuring.j2` - Структурирование документа

```python
from src.prompts.index import PromptLoader, PromptType

loader = PromptLoader()
context = {
    'user_input': 'Проанализируй договор',
    'extracted_text': contract_text,
    'my_company': {'inn': '...', 'name': '...'},
    'counterparty': {'inn': '...', 'name': '...'}
}

prompt = loader.render_prompt(PromptType.RISK_TABLE, context)
# Готовый промпт с подставленными данными
```

### ✅ Модуль 5: Генерация документов (Document Generation)
- **DOCX**: Генерация через docxtpl с поддержкой шаблонов
- **XLSX**: Таблицы через pandas + openpyxl
- Возврат как байтов, так и сохранение в файл

```python
from src.files.index import DocumentGenerator

generator = DocumentGenerator()

# XLSX
risks_data = {
    'columns': ['Риск', 'Уровень', 'Рекомендация'],
    'rows': [['...', 'High', '...']]
}
xlsx_bytes = generator.generate_xlsx(risks_data)

# DOCX
docx_bytes = generator.generate_docx(context={'content': '...'})
```

### ✅ Модуль 6: Структуры данных (Enhanced Schema)
- **CaseContext**: Контекст юридического кейса
- **FileMetadata**: Метаданные файла с extracted_text
- **Session**: Расширенная модель сессии с контекстом

```python
from src.db.index import CaseContext, FileMetadata

context = CaseContext(
    my_company={'inn': '...', 'name': '...'},
    counterparty={'inn': '...', 'name': '...'},
    files=[FileMetadata(...)],
    scenario='risk_table'
)
```

## 🧪 Тестирование

**36 тестов** покрывают все модули:

```bash
python3 -m unittest discover tests -v
```

- **10 тестов** - Security (контроль доступа)
- **9 тестов** - File Processing (извлечение текста)
- **10 тестов** - Router (маршрутизация)
- **7 тестов** - Prompts (шаблоны)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Основные зависимости:**
- `python-docx` - DOCX парсинг
- `PyMuPDF` - PDF текст
- `jinja2` - Шаблонизация
- `docxtpl` - DOCX генерация
- `pandas`, `openpyxl` - XLSX генерация

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и настройте:

```env
# Безопасность
ALLOWED_CHAT_IDS=123456789,987654321

# LLM (для будущей интеграции)
LLM_API_KEY=your_key
LLM_MODEL=gpt-4

# DaData (для будущей интеграции)
DADATA_TOKEN=your_token
```

### 3. Запуск демо

```bash
python3 examples/integration_demo.py
```

Демо покажет:
1. ✅ Контроль доступа
2. ✅ Извлечение текста из документа
3. ✅ Определение сценария по запросу
4. ✅ Генерацию промпта с контекстом
5. ✅ Создание кейса
6. ✅ Генерацию выходных файлов
7. ✅ Уточняющие вопросы

## 📋 Канонические сценарии

1. **🧱 Структура документа** - Анализ структуры юридического документа
2. **🔍 Подготовка к спору** - Сбор аргументов и доказательств
3. **✍️ Юридическое заключение** - Профессиональное мнение
4. **⚖️ Объяснение клиенту** - Простым языком о сложном
5. **📬 Ответ на претензию** - Официальный ответ контрагенту
6. **📋 Деловая переписка** - Контекст для писем
7. **🧩 Договор (РФ)** - Проверка договора по ГК РФ
8. **📑 Таблица рисков** - Анализ рисков договора
9. **📊 Судебная практика** - Анализ прецедентов

## 🏗️ Архитектура

```
src/
├── security/          # Модуль безопасности
│   ├── __init__.py
│   └── access_control.py
├── files/            # Обработка и генерация файлов
│   └── index.py
├── router/           # Интеллектуальная маршрутизация
│   └── index.py
├── prompts/          # Канонические промпты
│   ├── index.py
│   └── templates/    # Jinja2 шаблоны
│       ├── risk_table.j2
│       ├── contract_agent_rf.j2
│       ├── claim_response.j2
│       └── legal_document_structuring.j2
├── db/               # Модели данных
│   └── index.py
├── llm/              # LLM интеграция (TODO)
│   └── index.py
├── integrations/     # DaData и др. (TODO)
│   └── index.py
└── bot/              # Telegram бот (TODO)
    └── index.py
```

## 🔄 Дорожная карта

### ✅ Фаза 1: Фундамент (Завершена)
- [x] Security middleware с whitelist
- [x] Извлечение текста из PDF/DOCX
- [x] Генерация DOCX/XLSX
- [x] Расширенная схема данных

### ✅ Фаза 2: Интеллект (Завершена)
- [x] Детерминированный роутер
- [x] Jinja2 промпты
- [x] Система уточняющих вопросов
- [x] 36 unit тестов

### 🔜 Фаза 3: Интеграции
- [ ] LLM клиент (OpenAI/Anthropic)
- [ ] DaData интеграция
- [ ] Скоринг контрагента

### 🔜 Фаза 4: Telegram Bot
- [ ] Webhook handlers
- [ ] Команды /start, /help, /memory_clear
- [ ] Обработка файлов через Telegram
- [ ] Отправка готовых документов

## 📚 Документация

### Использование модулей

**Полный пример workflow:**

```python
# 1. Проверка доступа
from src.security.access_control import AccessController
controller = AccessController()
if not controller.is_allowed(user_id):
    return "⛔ Доступ запрещён"

# 2. Извлечение текста из файла
from src.files.index import FileProcessor
processor = FileProcessor()
text = processor.extract_text(file_path)

# 3. Определение сценария
from src.router.index import Router
router = Router()
scenario, confidence = router.route(user_query, context={'has_file': True})

# 4. Генерация промпта
from src.prompts.index import PromptLoader
loader = PromptLoader()
prompt = loader.render_prompt(scenario, {
    'user_input': user_query,
    'extracted_text': text,
    'my_company': {...},
    'counterparty': {...}
})

# 5. Отправка в LLM (TODO)
# response = llm_client.complete(prompt)

# 6. Генерация выходного документа
from src.files.index import DocumentGenerator
generator = DocumentGenerator()
if scenario == Scenario.RISK_TABLE:
    xlsx = generator.generate_xlsx(risks_data)
    # Отправить пользователю
```

## 🧑‍💻 Разработка

### Добавление нового сценария

1. Добавьте сценарий в `src/router/index.py`:
```python
class Scenario(Enum):
    NEW_SCENARIO = "new_scenario"
```

2. Добавьте hard gate в Router:
```python
HARD_GATES = {
    Scenario.NEW_SCENARIO: [r'ключевые\s+слова'],
}
```

3. Создайте шаблон `src/prompts/templates/new_scenario.j2`

4. Добавьте тесты в `tests/`

### Добавление нового промпта

Создайте файл `src/prompts/templates/my_scenario.j2`:

```jinja2
Ты — эксперт. Задача: {{ task }}

{% if extracted_text %}
Документ: {{ extracted_text[:5000] }}
{% endif %}

Инструкции...
```

## 📄 Лицензия

MIT

## 👥 Авторы

ArtemFilin1990
