# Конфигурация для Amvera
# Указываем containerPort, т.к. приложение слушает порт 3000, а не 80

toolchain:
  name: docker

run:
  # Порт, который слушает контейнер (должен совпадать с ENV PORT в Dockerfile)
  containerPort: 3000
